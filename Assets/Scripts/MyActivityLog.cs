using UnityEngine;
using System.Collections;
using UnityEngine.UI;

public class MyActivityLog : MonoBehaviour {

	// Use this for initialization
	void Start () {
	gameObject.SetActive(false);
	}
	
	// Update is called once per frame
	void Update () {
	
	}
}
